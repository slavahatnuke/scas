module.exports = class Scas {
    constructor() {
        
    }

    handle(request) {
        return Promise.resolve()
            .then(() => {
                // console.log(request);
            });
    }
}
